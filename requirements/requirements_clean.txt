# Requirements per Llama RAG Chatbot - 4GB VRAM
# Installazione su D:\venv_spirituality

# PyTorch con CUDA 12.1 (solo torch, NO torchvision/torchaudio)
--index-url https://download.pytorch.org/whl/cu121
torch==2.5.1

# Transformers + Tokenizers (versioni compatibili)
transformers==4.36.0
tokenizers==0.15.2

# Quantizzazione 4-bit
accelerate==0.25.0
bitsandbytes==0.41.3

# Embeddings RAG
sentence-transformers==3.0.1
scikit-learn==1.3.2

# LoRA/QLoRA per fine-tuning (opzionale ma consigliato)
peft==0.7.1

# Dipendenze base (già incluse ma specifichiamo versioni stabili)
numpy>=1.24.0,<2.0.0
scipy>=1.10.0,<1.12.0
huggingface-hub>=0.19.0
safetensors>=0.4.0

# Backend API Flask
flask==3.0.0
flask-cors==4.0.0
flask-limiter==3.5.0
redis==5.0.1
gunicorn==21.2.0